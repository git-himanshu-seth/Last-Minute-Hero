# Google Gemini AI API Configuration
GEMINI_API_KEY=

# MongoDB Connection Configuration (Optional - Falls back gracefully to Firestore if empty)
MONGODB_URI=
MONGODB_DB_NAME=lifesaver_ai

# Stripe API Configuration (Used for actual billing; falls back gracefully to a robust sandbox simulation if empty)
STRIPE_SECRET_KEY=
